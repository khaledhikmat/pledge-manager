kubectl delete \
    -f functions.yaml \
    -f users.yaml \
    -f campaigns.yaml \
    -f campaigns-pubsub.yaml \
    -f campaigns-store.yaml \
    -f redis.yaml \
    -f zipkin.yaml \
    -f dapr-config.yaml \
    -f secrets.yaml \
    -f namespace.yaml \
